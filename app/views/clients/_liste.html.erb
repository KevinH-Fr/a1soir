
 <strong> Clients</strong>
 nb: <%= Client.count %>
 <%= link_to "Nouveau", new_client_path, class:"btn btn-sm btn-primary" %>

 <table class="table table-primary table-striped table-bordered table-hover">
    <tbody class="table-light">
      <tr>
        <% @clients.each do | client | %>
          <td> <%= link_to client.nom, client_path(client), class:"text-dark" %> </td>
        </tr> 
        <% end %>
    </tbody>
 </table>