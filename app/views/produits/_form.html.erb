<%= turbo_frame_tag "produit_form" do %>

  <div class="card mx-2 mt-3 p-2 light-beige-colored shadow-sm">

    <%= form_with(model: produit) do |form| %>
      <% if produit.errors.any? %>
        <div style="color: red">
          <h2><%= pluralize(produit.errors.count, "error") %> prohibited this produit from being saved:</h2>

          <ul>
            <% produit.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= custom_form_elements(form, 
        [:nom, :text_field], [:prixvente, :text_field], [:prixlocation, :text_field],
        [:description, :text_area], [:categorie_produit_id, :text_field], [:caution, :text_field],
        [:handle, :text_field], [:reffrs, :text_field], [:quantite, :number_field],
        [:fournisseur_id, :text_field], [:dateachat, :date_field], [:prixachat, :text_field]
      ) %>

    <% end %>
  
  </div>

<% end %>
