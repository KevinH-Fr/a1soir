<%= bandeau_entete("Couleurs", "paint", @couleurs.count, :nom_cont) %>

<%= bloc_nouveau(Couleur) %>

<div id="couleurs">
  <% @couleurs.each do |couleur| %>
        <%= render couleur %>
  <% end %>
</div>


