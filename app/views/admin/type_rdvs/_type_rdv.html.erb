<div id="<%= dom_id(type_rdv) %>" class="card m-2">
  <div class="card-body">
    <div class="row align-items-center">
      <div class="col-md-8">
        <strong>Nom:</strong> <%= type_rdv.code %>
        <strong>Durée:</strong> <%= type_rdv.duree_base_minutes %> minutes
      </div>
      <div class="col-md-4 text-end">
        <%= links_record(type_rdv) %>
      </div>
    </div>
  </div>
</div>
