<%= card_main_model("Ensemble", "collection") %>

<div class="<%= admin_list_stack_classes %>">
  <%= render @ensemble %>
</div>

<hr>

<%= return_model_index_button("Retour vers Ensembles", admin_ensembles_path) %>
