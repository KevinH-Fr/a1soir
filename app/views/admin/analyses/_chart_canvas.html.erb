<%# locals: chart_id, config, extra_class: nil %>
<div class="<%= analyses_chart_box_classes(extra_class) %>">
  <canvas
    id="<%= chart_id %>"
    data-analyses-chart="<%= config.to_json %>"
    role="img"
    aria-label="<%= local_assigns[:aria_label] || "Graphique" %>"
  ></canvas>
</div>
