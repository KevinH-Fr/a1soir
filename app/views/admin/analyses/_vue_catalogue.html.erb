<% if analyses_visibility_show?(:kpi) %>
  <%= render_analyses_kpi_section %>
<% end %>

<% if analyses_visibility_show?(:catalog_section) %>
  <div class="mt-3">
    <%= render "catalog_section" %>
  </div>
<% end %>
