<%# locals: text, extra_class: nil %>
<p class="mensuration-form__advice text-light opacity-75 small mb-0<%= " #{extra_class}" if extra_class.present? %>">
  <i class="bi bi-info-circle mensuration-form__advice-icon" aria-hidden="true"></i>
  <span><%= text %></span>
</p>
