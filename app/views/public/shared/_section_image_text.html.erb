<!-- Section avec image et texte -->
<div class="container my-5 py-5">
  <!-- Titre optionnel -->
  <% if local_assigns[:title].present? %>
    <div class="section-title-wrapper mb-5">
      <h2 class="section-title text-light fw-bold" style="font-family: 'Playfair Display', serif; font-size: 2.5rem; position: relative; display: inline-block;">
        <%= title %>
        <span class="section-title-underline" style="background: white;"></span>
      </h2>
    </div>
  <% end %>
  
  <div class="row align-items-center g-4 g-lg-5">
    <!-- Image à gauche -->
    <div class="col-12 col-lg-5" data-scroll-reveal>
      <div class="position-relative overflow-hidden rounded-0 shadow-lg" 
           style="height: <%= local_assigns[:image_height] || '450px' %>;">
        <%= image_tag(image_path, 
            class: "w-100 h-100", 
            style: "object-fit: cover; object-position: #{local_assigns[:image_position] || 'center'};") %>
        <div class="position-absolute top-0 start-0 w-100 h-100" 
             style="background: linear-gradient(to bottom, rgba(0,0,0,0.1), rgba(0,0,0,0.3));"></div>
      </div>
    </div>
    
    <!-- Texte à droite -->
    <div class="col-12 col-lg-7" data-scroll-reveal>
      <div>
        <p class="lead text-light opacity-75" style="font-size: 1.15rem; line-height: 1.8;">
          <%= description %>
        </p>
        
        <!-- Points avec check (optionnel) -->
        <% if local_assigns[:features].present? && features.any? %>
          <div class="mt-4 d-flex align-items-center gap-4 flex-wrap">
            <% features.each do |feature| %>
              <div class="d-flex align-items-center">
                <i class="bi bi-check-circle-fill text-light me-2" style="font-size: 1.5rem; color: #d4af37;"></i>
                <span class="text-light"><%= feature %></span>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

